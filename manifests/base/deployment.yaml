apiVersion: apps/v1
kind: Deployment
metadata:
  name: istio-test-app
  namespace: istio-app
  labels:
    app: istio-test-app
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: istio-test-app
      version: v1
  template:
    metadata:
      labels:
        app: istio-test-app
        version: v1
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      serviceAccountName: istio-test-app
      containers:
      - name: app
        image: nginx:1.21
        ports:
        - containerPort: 80
          name: http
        env:
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: HOSTNAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
        securityContext:
          runAsNonRoot: false
          allowPrivilegeEscalation: false
        volumeMounts:
        - name: html-content
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html-content
        configMap:
          name: nginx-html-config
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-html-config
  namespace: istio-app
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
        <title>EKS Istio Test Application</title>
        <style>
            body { 
                font-family: Arial, sans-serif; 
                margin: 40px;
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                color: white;
                text-align: center;
            }
            .container {
                background: rgba(255,255,255,0.1);
                padding: 30px;
                border-radius: 10px;
                backdrop-filter: blur(10px);
            }
            h1 { color: #ffeb3b; }
            .status { 
                padding: 10px; 
                border-radius: 5px; 
                margin: 10px 0; 
                background: rgba(255,255,255,0.2);
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>🚀 EKS Istio Test Application</h1>
            <p><strong>Great job ----WOW WOW MANMOHAN RULES!</strong></p>
            
            <div class="status">
                <h2>📊 Application Status</h2>
                <p><strong>Istio:</strong> ✅ Enabled with mTLS</p>
                <p><strong>Gateway:</strong> ✅ Public Access</p>
                <p><strong>Security:</strong> ✅ Authorization Policies</p>
            </div>
            
            <div class="status">
                <h2>🔒 Security Features</h2>
                <p>• ✅ Istio mTLS enabled (STRICT mode)</p>
                <p>• ✅ Authorization policies applied</p>
                <p>• ✅ Istio Gateway for public access</p>
                <p>• ✅ Service mesh communication secured</p>
            </div>
            
            <div class="status">
                <h2>🎯 Next Steps</h2>
                <p>• Connect to PostgreSQL database</p>
                <p>• Add REST API endpoints</p>
                <p>• Implement full application features</p>
            </div>
            
            <div class="status">
                <h2>🎉 Success!</h2>
                <p><strong>Istio Service Mesh + mTLS + Gateway = Working!</strong></p>
                <p>Complete EKS + Istio + PostgreSQL integration ready!</p>
            </div>
        </div>
    </body>
    </html>
